node_modules/
test/out/
*.log

# Live work-loop scratch — regenerated constantly, not source of truth
progress.html
.critic/

# orthograph.html IS tracked. It is the whole pitch — one file you open in a
# browser — so it ships in the repo. It was ignored only while four parallel
# worktrees were in flight, because every one of them regenerates it. If a
# future merge conflicts on it, the resolution is always `node build.js`.

# editor/agent scratch, not project config
.claude/
